{
  "Version": "2012-10-17",
  "Statement": [{
    "Sid": "ReadWriteSingleVolume",
    "Effect": "Allow",
    "Action": ["s3:GetObject", "s3:PutObject", "s3:DeleteObject"],
    "Resource": [
      "arn:aws:s3:::{{env.bucket}}/by_id/{{req.volume}}/*",
      "arn:aws:s3:::{{env.bucket}}/meta/{{req.volume}}.provenance",
      "arn:aws:s3:::{{env.bucket}}/meta/{{req.volume}}.pub"
    ],
    "Condition": {
      "DateLessThan": {"aws:CurrentTime": "{{mint.expiry}}"}
    }
  }]
}
